#include <iostream>
using namespace std;

int main() {
    long long n;
    if (!(cin >> n)) return 0;
// Write your code after this line
    // if (n % 2 == 0) cout << n << " is even." << endl;
    // else            cout << n << " is odd." << endl;
    return 0;
}
